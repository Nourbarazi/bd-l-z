{
  "homepage": "https://nourbarazi.github.io/bd-l-z/",
  "name": "lz-birthday",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "author": "Nour Albarazi",
  "license": "ISC",
  "scripts": {
    "start": "vite --open",
    "build": "npm run fix &&  vite build",
    "preview": "npm run build && vite preview",
    "fix": "prettier --write .",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "gsap": "^3.12.5",
    "sass": "^1.77.5",
    "split-type": "^0.3.4",
    "vite": "^5.3.1"
  },
  "devDependencies": {
    "prettier": "^3.3.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
